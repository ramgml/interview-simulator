# Верификация релиза v0.2.0

> Прогон чеклиста «Верификация MVP» (`docs/ARCHITECTURE.md`, раздел «Верификация MVP») на базе `origin/develop` (8469e04, T177), 2026-09-08. Окружение: локальный omniroute gateway `glm/glm-5.3-flash` (`:20128`), backend uvicorn `:8100`, frontend dev `:3000`, один и тот же коммит для дев-инстанса и релизной ветки (релизная ветка на момент прогона отличается только bump'ом версий).

## Результаты по пунктам чеклиста

| # | Пункт | Результат | Доказательство |
|---|---|---|---|
| 1 | `make setup`, `make models` | Выполнено ранее (эпики T121–T126); STT/TTS модели в кэше, работают живьём (п. 6) | Прогон п. 6 |
| 2 | `make test` — pytest зелёный | **170 passed** | Прогон 2026-09-08, 11 c (develop до ветвления; на релизной ветке — повторно, см. «Гейты») |
| 3 | `make mlflow-ui` → `:5100`; `make dev` → `:3000`, `:8100/docs` | Бэк: `/health` → `{"status":"ok"}`, `/docs` → **200**; фронт: HTTP **200**, `<title>Тренажёр собеседований</title>` | curl-прогоны 2026-09-08 |
| 4 | E2E-сценарий: сессия → интервью → отчёт | Создана сессия middle/friendly/5 (вакансия «Бэкенд-разработчик Python (FastAPI, SQL)»): PLAN распознал позицию; вопросы тематические (asyncio event loop → SQL JOIN/индексы); текстовый ответ принят; `/finish` → отчёт `overall_score 5.0`, компетенции с оценками и комментариями, `status=completed` | curl-прогон 2026-09-08, сессия `22ea4323…3583` (синтетическая, помечена «Тест релизной верификации v0.2.0») |
| 5 | MLflow: run, артефакты, трейсы | У сессии заполнен `mlflow_run_id` (`464d6160fe8a47a593e443f36986f325`) | Чтение `data/app.db`, 2026-09-08 |
| 6 | TTS/STT | `POST :8100/api/tts` → **WAV PCM 16-bit mono 24 kHz**; round-trip TTS→STT: «Проверка синтеза речи релиза 0.2.» confidence 0.83 | `file /tmp/tts-check.wav`; curl-прогоны |
| 7 | Настройки | `/api/settings/test` → `{"ok":true}`; эффективный провайдер отдаёт модель `glm-5.3-flash`; ходы интервью прошли без рестарта | curl-прогоны |
| 8 | Прогресс | `/api/progress` содержит завершённые сессии, включая свежую (score 5.0); `trend: null` на текущих данных | curl `/api/progress` |
| 9 | Ошибочный контракт | `POST /answer` по completed-сессии → **409** с осмысленным `detail` | curl-прогон |
| 10 | Push в `main` | Выполняется мержем релизного PR владельцем | GitFlow (W201) |

## Гейты на релизной ветке (после bump 0.1.0 → 0.2.0)

- `ruff check .` — All checks passed (backend).
- `npx tsc --noEmit` — без ошибок (frontend).
- `pytest` — 170 passed.

## Изменения v0.1.0 → v0.2.0 (12 смерженных PR, develop 50bef9f → 8469e04)

- T152 fix: LLM-модель/провайдер; T155 fix: SQLite WAL + busy_timeout; T156 fix: MLflow lifespan; T157: max_tokens из конфига.
- T158/T162: отмена сессии + guard статуса после LLM-вызова (race cancel).
- T159: список моделей от провайдера (`/api/models`).
- T163: настройки аудио — выбор микрофона и устройства вывода с проверкой (+ user story №9 в PRD).
- T169 fix: остановка mic-стрима при unmount, ошибка теста звука; T170: кнопка «Настройки» на главной.
- T172: toggle-запись в Recorder (клик старт/стоп) + guard race двойного клика.
- T173: layout отчёта — переносы длинных бейджей, полноширинный адаптив.
- T177: CORS `allow_origins` из конфига (`CORS_ORIGINS`, CSV).
- Версии синхронно подняты: `backend/pyproject.toml`, `backend/uv.lock`, `frontend/package.json`, `frontend/package-lock.json` → 0.2.0.

## Наблюдения прогона (вне скоупа, кандидаты в backlog)

1. Тяжёлый EVAL на `/finish` занял ~2.5 мин (полный транскрипт одним промптом через локальный шлюз); отчёт валиден, degraded-фоллбек не понадобился.
2. `/api/progress`: `trend` = `null` на текущем наборе сессий (см. п. 8) — не блокер, покрыто юнит-тестами.
3. Предыдущий релизный отчёт (`VERIFICATION-v0.1.0.md`) сохранён как история; его «известные ограничения» п. 1 (SQLite lock) закрыты T155 (WAL), п. 2 (rate-limit EVAL) — не воспроизводился в этом прогоне.
